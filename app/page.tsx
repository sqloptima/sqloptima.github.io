import Link from "next/link";
import { listContent } from "@/lib/content";

export default function Home() {
  const featured = [...listContent("handbook").slice(0, 3), ...listContent("blog").slice(0, 3)];
  return <main><section className="hero"><div className="wrap"><p className="eyebrow">Open database engineering library</p><h1>Practical SQL knowledge for production systems.</h1><p className="lede">Free articles, operational handbooks, checklists, and SQL scripts for SQL Server and PostgreSQL practitioners.</p><div className="actions"><Link className="button" href="/handbook/">Explore the handbook</Link><Link className="button secondary" href="/knowledgebase/">Browse scripts and checklists</Link></div></div></section><section className="wrap section"><p className="eyebrow">Featured guides</p><h2>Start with proven operational patterns</h2><div className="cards">{featured.map((item) => { const collection = listContent("handbook").some((x) => x.slug === item.slug) ? "handbook" : "blog"; return <Link className="card" href={`/${collection}/${item.slug}/`} key={`${collection}-${item.slug}`}><small>{collection} · {item.date}</small><h2>{item.title}</h2><p>{item.summary}</p></Link>; })}</div></section><section className="wrap section"><p className="eyebrow">Practice areas</p><h2>Build reliable database habits</h2><div className="cards">{[["Availability","Failover, recovery objectives, and rehearsed disaster recovery."],["Performance","Query plans, waits, indexes, and measurable tuning."],["Administration","Backups, capacity, maintenance, and daily operational checks."],["Development","Schema design, SQL patterns, and application contracts."],["Security","Access, auditing, and protection of sensitive data."],["AI and data","Careful ways to connect AI workflows to governed databases."]].map(([title,summary]) => <Link className="card" href={`/practice/#${title.toLowerCase().replaceAll(" ","-")}`} key={title}><h2>{title}</h2><p>{summary}</p></Link>)}</div></section></main>;
}
